const pg = require('utils/pg')

module.exports = async function getUserId ({ username }) {
  const { rows: [ { user_id } ] } = await pg.query(/*sql*/`
    SELECT account.user.user_id
    FROM account.user
    WHERE account.user.username = $1
  `, [ username ])

  return user_id
}
