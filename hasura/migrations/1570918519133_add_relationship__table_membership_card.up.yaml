- args:
    name: prints
    table:
      name: card
      schema: membership
    using:
      foreign_key_constraint_on:
        column: card_id
        table:
          name: print
          schema: membership
  type: create_array_relationship
